---
const { ...props } = Astro.props;
---

<pre
    class="h-screen font-mono text-gray-600 -mt-18 mx-auto text-center -z-20"
    style="
          background-image: linear-gradient(
          rgba(180, 100, 20, 0.7),
            rgba(0, 20, 200, 0.7)
          );
        ">
</pre>
<script is:inline type="module">
    import { run } from "/ajaibghar-landing-page/scripts/run.js";
    import * as program from "/ajaibghar-landing-page/scripts/program/new.js";
    // Run settings can override the default- and program seetings.
    // See the API for details.
    const settings = {
        fps: "14",
        element: document.querySelector("pre"),
        fontSize: "1em",
    };

    // Boot (returns a promise)
    run(program, settings).catch(function (e) {
        console.warn(e.message);
        console.log(e.error);
    });
</script>
